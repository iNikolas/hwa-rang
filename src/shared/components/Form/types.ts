export type FormSchema = {
  name: string;
  service: string;
  hall: string;
  for: string;
  age: string;
  howToConnect: string[];
  phone: string;
};

export type Choice = {
  trainer: string;
  selectedHall: string;
};
